# 任务清单

## 1. 用途

本清单用于：

- 让 Claude Code 按任务推进实现
- 让 Claude Code 在每个任务完成后更新进度、自测结果和阻塞信息
- 让 codex 基于统一清单进行 review 和验收前检查

使用要求：

- Claude Code 每完成一个任务，必须更新本文件
- 任务状态必须真实反映当前进度
- 不允许只改代码不更新清单
- 旧目标遗留任务不得删除，只能归档说明

当前默认开发测试摄像头：

- 名称：电梯厅
- IP：`192.168.77.117`
- 验证码：`ACNPUB`
- 编码：`H265`
- 分辨率：`2304x1296`

当前一期核心规则：

- 目标人物：`杨孝治`
- 目标摄像头：`电梯厅`
- 目标时段：夜间与清晨，以配置为准
- 核心动作：`电话告警 + 本地日志 + 证据保存`

## 2. 状态定义

- `未开始`：尚未动手
- `进行中`：正在实现或调试
- `待Review`：代码和自测已完成，等待 codex review
- `需返工`：review 后发现问题，需要继续修改
- `已完成`：review 通过或确认当前阶段完成
- `已阻塞`：因环境、依赖或需求问题无法继续

## 3. 更新规则

Claude Code 每次更新任务时，至少要填写：

- 状态
- 更新日期
- 修改文件
- 自测命令
- 自测结果
- 风险/阻塞

codex review 时会补：

- Review 结论
- Review 备注

## 4. 当前阶段任务总览

### 4.1 旧目标基础能力归档

以下任务是上一阶段“单机陌生人文字告警”目标下的基础能力沉淀，可复用，但不代表已经满足当前“老人夜间外出监护告警”目标：

| 编号 | 任务名称 | 状态 | 负责人 | 最近更新 |
| --- | --- | --- | --- | --- |
| T01 | 初始化项目骨架 | 已完成 | Claude Code | 2026-04-08 |
| T02 | 配置加载与校验（旧结构） | 已完成 | Claude Code | 2026-04-08 |
| T03 | 告警时段判断 | 已完成 | Claude Code | 2026-04-08 |
| T04 | 证据磁盘配额清理 | 已完成 | Claude Code | 2026-04-08 |
| T05 | 家人人脸目录扫描 | 已完成 | Claude Code | 2026-04-08 |
| T06 | 视频流连接与重连控制 | 已完成 | Claude Code | 2026-04-08 |
| T07 | 告警冷却策略 | 已完成 | Claude Code | 2026-04-08 |
| T08 | 命令行文字告警输出 | 已完成 | Claude Code | 2026-04-08 |
| T09 | 陌生人事件聚合 | 已完成 | Claude Code | 2026-04-08 |
| T10 | 监控编排主流程（旧目标） | 已完成 | Claude Code | 2026-04-08 |
| T11 | 启动检查模式（旧目标） | 已完成 | Claude Code | 2026-04-08 |
| T12 | 操作文档与手工验证指南（旧目标） | 已完成 | Claude Code | 2026-04-08 |

### 4.2 当前目标任务

| 编号 | 任务名称 | 状态 | 负责人 | 最近更新 |
| --- | --- | --- | --- | --- |
| T13 | 配置模型切换到目标人物监护规则 | 待Review | Claude Code | 2026-04-08 |
| T14 | 目标人物识别与多帧投票 | 待Review | Claude Code | 2026-04-08 |
| T15 | 电话告警适配层 | 待Review | Claude Code | 2026-04-08 |
| T16 | 监控编排切换到”杨孝治夜间外出”规则 | 待Review | Claude Code | 2026-04-08 |
| T17 | 启动检查、自测与手工验证切换 | 待Review | Claude Code | 2026-04-09 |
| T18 | 文档收尾与交付同步 | 未开始 | Claude Code | 2026-04-08 |

---

## T13 配置模型切换到目标人物监护规则

- 状态：待Review
- 更新日期：2026-04-08
- 修改文件：`src/config.py`, `tests/test_config.py`, `config.example.yaml`
- 自测命令：`pytest tests/test_config.py -v`
- 自测结果：21 passed（完整加载、profiles、phone_alert、alert参数、跨天时段、名称重复报错、无monitor_rules报错、缺杨孝治规则报错、person_name不在profiles报错、缺person_name报错、空actions报错、phone_alert缺provider/template/numbers各报错、phone_alert未启用报错、phone_alert缺失报错、非法时间格式报错、时间超范围报错、storage非法报错、缺顶层字段报错、旧配置格式明确失败）
- 风险/阻塞：旧配置格式不兼容，已明确报错；config.example.yaml 已同步更新到新结构
- 返工修复：(1) phone_alert 改为必填且 enabled 必须为 true，缺失或禁用均启动失败；(2) monitor_rules 必须包含至少一条 person_name="杨孝治" 的规则，且 person_name 必须在 profiles 中存在
- Review 结论：待 codex review
- Review 备注：当前需求要求引入 `monitor_rules`、`profiles`、`phone_alert`，并以”杨孝治 + 电梯厅 + 夜间时段”为主规则。

## T14 目标人物识别与多帧投票

- 状态：待Review
- 更新日期：2026-04-08
- 修改文件：`src/vision.py`（重写）, `src/face_registry.py`（补 ensure_target_person_exists）, `tests/test_vision.py`（重写 11 个）, `tests/test_face_registry.py`（补 2 个）, `src/monitor.py`（最小适配新 API，T16 完整重构）
- 自测命令：`pytest tests/test_vision.py tests/test_face_registry.py -v` + `pytest -q`
- 自测结果：vision 11 passed + face_registry 8 passed；全量 89 passed
- 风险/阻塞：monitor.py 中的临时适配（_try_recognize_family 返回 bool，而非人名）需在 T16 完整重构；辅助画像仅保留 profiles 接口，未做加权逻辑
- Review 结论：待 codex review
- Review 备注：当前目标不是”陌生人聚合”，而是”杨孝治命中判定”；要求连续多帧投票，单帧不能直接触发。

## T15 电话告警适配层

- 状态：待Review
- 更新日期：2026-04-08
- 修改文件：`src/phone_alert.py`（新建）, `tests/test_phone_alert.py`（新建）
- 自测命令：`pytest tests/test_phone_alert.py -v` + `pytest -q`
- 自测结果：phone_alert 11 passed
- 风险/阻塞：阿里云 VMS SDK 尚未接入，AliyunVmsClient.call() 当前返回 success=False + 明确错误信息"尚未接入"；真实电话拨打能力需后续接入 SDK 后才可用；自动化测试全部基于 mock，不依赖外部服务
- 返工修复：(1) AliyunVmsClient.call() 从返回 success=True 改为 success=False，SDK 未接入时不假装成功；(2) mock 工厂支持从配置控制 mock_should_succeed/mock_error_message，便于后续集成测试失败路径
- Review 结论：待 codex review
- Review 备注：电话告警是当前一期核心能力，不允许再降级为”仅终端文字提醒”。

## T16 监控编排切换到”杨孝治夜间外出”规则

- 状态：待Review
- 更新日期：2026-04-08
- 修改文件：`src/monitor.py`（重写）, `src/notifier.py`（重写）, `tests/test_monitor.py`（重写 19 个）, `tests/test_notifier.py`（重写 8 个）
- 自测命令：`pytest tests/test_monitor.py tests/test_notifier.py -v` + `pytest -q`
- 自测结果：vision 11 + monitor 17 + notifier 8 + config 22 = 全量 107 passed
- 风险/阻塞：_try_identify_person 使用 face_recognition 全分辨率检测，在监控距离下人脸检测准确率仍待真机验证；阿里云 VMS SDK 尚未接入（AliyunVmsClient 返回 success=False）
- 返工修复：(1) PersonHitWindow 拆分为 record/is_hit/consume 三步，非告警时段命中不被消费；(2) 冷却键改为 `camera:rule` 维度，同摄像头多规则互不影响；(3) 配置阶段强制要求 actions 包含 phone_call
- Review 结论：待 codex review
- Review 备注：最终主流程应表现为”电梯厅 + 夜间时段 + 杨孝治 -> 电话告警 + 证据保存 + 本地日志”。

## T17 启动检查、自测与手工验证切换

- 状态：待Review
- 更新日期：2026-04-09
- 修改文件：`tests/test_main_cli.py`（重写 8 个）, `docs/manual-test.md`（重写）
- 自测命令：`pytest tests/test_main_cli.py -v` + `pytest -q`
- 自测结果：cli 8 passed；全量 109 passed
- 风险/阻塞：阿里云 VMS SDK 尚未接入；run_check 通过 readiness_status() 如实输出”客户端可初始化，但阿里云 VMS SDK 尚未接入，当前不能真实拨打”
- 返工修复：phone_alert 接口新增 readiness_status()，AliyunVmsClient 返回骨架状态描述，run_check 使用该接口替代”初始化成功”表述
- Review 结论：待 codex review
- Review 备注：默认真机联调摄像头必须切到”电梯厅”。

## T18 文档收尾与交付同步

- 状态：未开始
- 更新日期：2026-04-08
- 修改文件：待 Claude Code 更新
- 自测命令：待 Claude Code 更新
- 自测结果：待 Claude Code 更新
- 风险/阻塞：主文档口径必须统一；不得保留“当前一期不做电话告警”之类旧表述
- Review 结论：待 codex review
- Review 备注：README、启动提示词、首条消息、项目记忆等都需要同步切到当前目标。
