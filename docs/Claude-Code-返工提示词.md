# Claude Code 返工提示词

把下面这段发给 Claude Code：

```text
请不要继续新功能开发，先处理 codex 在 docs/任务清单.md 里标出的 review 结果。

当前需要返工的任务只有这 4 项：

1. T02 配置加载与校验
2. T04 证据磁盘配额清理
3. T10 监控编排主流程
4. T12 操作文档与手工验证指南

请严格按下面要求执行：

一、先读这些文件
1. docs/任务清单.md
2. docs/需求文档.md
3. docs/claude-code-执行说明.md
4. docs/自测清单.md

二、逐项处理返工

T02：
- 在配置加载阶段校验告警时段格式
- 非法时间字符串必须在启动时明确报错
- 不能等到 scheduler 运行时才失败
- 补充对应测试

T04：
- 统一“证据删除顺序”的实现和文档口径
- 现在实现与需求描述不一致
- 修复后补充或调整测试，确保规格明确

T10：
- 不能在缺少 face_recognition 时静默退化成“所有有人画面都算陌生人”
- 当前需求要求区分家人与陌生人
- 请修复运行时行为，使之与需求一致
- 同时修复短视频拼接逻辑，避免告警后帧重复写入
- 补充针对监控主流程关键路径的测试，不要只测 should_alert_for_detection 这种纯布尔函数

T12：
- README 和 docs/manual-test.md 需要与修复后的实际行为保持一致
- 不要再把 face_recognition 写成“可选但不影响需求成立”

三、执行约束
- 不要修改需求目标
- 不要把返工任务之外的内容一起大改
- 每完成一个返工任务，更新 docs/任务清单.md
- 把对应任务状态从“需返工”改为“待Review”
- 在 Review 备注里保留 codex 之前的 review 结论，不要覆盖掉问题背景

四、完成后必须回报
- 修改了哪些文件
- 每个返工任务补了哪些测试
- 实际执行了哪些命令
- 哪些问题已经修复
- 是否还有剩余风险
```
